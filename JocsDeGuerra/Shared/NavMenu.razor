<MudNavMenu>
    <MudNavLink Href="/">Home</MudNavLink>
    <MudNavLink Href="/informetorn">Informe Torn</MudNavLink>
    <MudNavLink Href="/resumtorns">Resum Torns</MudNavLink>
    <MudNavLink Href="/informeestrategic">Informe estratègic</MudNavLink>
    <MudNavGroup Title="Llistes" @bind-Expanded=_isExpanded>
        <MudNavLink Href="locationlist">Localitzacions</MudNavLink>
        <MudNavLink Href="assetList">Assets</MudNavLink>
    </MudNavGroup>
    
</MudNavMenu>

@code {
    bool _isExpanded = false;
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
